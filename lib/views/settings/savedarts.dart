//TODO: here we have to display saved arts by the user

import 'package:flutter/material.dart';

class SavedArts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Saved arts"),);
  }
}
